1.

```
swagger init spec \
  --title "Rescale API" \
  --description "HPC Jobs Submitting Api" \
  --version 1.0.0 \
  --scheme http \
  --consumes application/json \
  --produces application/json

```
